import ru.evg.zakamen23.range.Range;

public class UseRange {
    public static void main(String[] args) {
        Range range1 = new Range(1, 8);
        Range range2 = new Range(8, 11);
        System.out.println(range1.intersect(range2));
        for (int i=0; i<2;++i){
            System.out.println(Arrays.toString(array));

        }
    }
}
